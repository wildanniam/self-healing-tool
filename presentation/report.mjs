import {ARMS,CATEGORIES,GROUPS} from './data.mjs';
import {RANKING_ARMS} from './ranking-data.mjs';
import {inspectorClient,INSPECTOR_CSS} from './inspector.mjs';
import {CSS} from './report-style.mjs';
const embed=x=>JSON.stringify(x).replaceAll('<','\\u003c').replaceAll('\u2028','\\u2028').replaceAll('\u2029','\\u2029');

export function renderReport(data){
  const studies=(data.studies??[{...data,studyId:'rm2'}]).map(s=>({...s,arms:s.arms??ARMS,
    rows:s.rows.map(r=>({...r,studyId:s.studyId,evidenceKind:s.evidenceKind,assessed:r.assessed??true}))}));
  const view={...data,studies,rows:studies.flatMap(s=>s.rows)};
  return `<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Self-healing · Hasil dan bukti</title><style>${CSS}${INSPECTOR_CSS}${POLISH}</style></head><body>
  <nav class="rail" aria-label="Report"><strong>Self-healing · Hasil dan bukti</strong><span id="generated"></span></nav>
  <main id="overview-view"><header><div class="topline"><div><p id="mode" class="eyebrow"></p><h1>Hasil pemulihan locator</h1></div><div class="actions"><button id="download">Unduh ringkasan JSON</button><button id="print">Cetak</button></div></div>
  <div class="report-navigation"><label id="study-label">Studi<select id="study">${studies.map(s=>`<option value="${s.studyId}">${s.studyId==='rm1'?'RM1 · Pemeringkatan DOM':'RM2 · Aturan target'}</option>`).join('')}</select></label><div role="group" aria-label="Tampilan"><button id="view-results" aria-pressed="true">Hasil eksekusi</button> <button id="view-comparison" aria-pressed="false">Perbandingan studi</button></div></div>
  <div id="coverage" role="status"></div><p id="scope" class="caption"></p><div class="stats" id="stats"></div></header>
  <section id="cases"><div class="filters"><label class="search-label">Cari kasus<input id="search" type="search" placeholder="ID atau tujuan tes"></label><label>Aplikasi<select id="group"><option value="all">Semua aplikasi</option>${Object.entries(GROUPS).map(([k,v])=>`<option value="${k}">${v}</option>`).join('')}</select></label><label>Kelompok<select id="category"><option value="all">Semua kelompok</option></select></label><label class="check"><input id="wrong-only" type="checkbox"> Kasus dengan tindakan salah</label></div>
  <div class="section-heading"><p id="case-count" class="caption" role="status"></p><button id="csv">Unduh CSV terfilter</button></div><p class="caption">Pilih hasil ulangan untuk melihat proses. ✓ benar menurut evaluator · ! tindakan salah · — berhenti · ? belum dinilai.</p>
  <div class="table-wrap"><table class="cases"><thead id="case-head"></thead><tbody id="case-rows"></tbody></table></div><div class="pagination"><button id="previous">← Sebelumnya</button><span id="page-number"></span><button id="next">Berikutnya →</button></div></section>
  <section id="comparison" hidden><h2 id="comparison-title"></h2><p class="caption">Seluruh data studi terpilih; filter daftar kasus tidak mengubah perbandingan ini.</p><div class="table-wrap"><table><thead id="metric-head"></thead><tbody id="metrics"></tbody></table></div><p class="caption">Eksekusi berulang bukan kasus independen. Pemulihan, penolakan dan kontrol tidak digabung menjadi satu success rate.</p><details><summary>Konfigurasi yang dibandingkan</summary><div id="arms"></div></details><details><summary>Token, waktu dan asumsi biaya</summary><div id="resources"></div></details></section>
  <details class="provenance"><summary>Sumber dan batas interpretasi</summary><p id="limitations"></p><pre id="provenance"></pre></details><footer>Hasil teknis, jawaban model, keputusan pemeriksa dan efek aplikasi adalah bukti yang berbeda.</footer></main><main id="inspector-root" hidden></main>
  <script>const data=${embed(view)};(${client.toString()})(data,${embed({ARMS:{...ARMS,...RANKING_ARMS},CATEGORIES,GROUPS})},${inspectorClient.toString()});</script></body></html>`;
}

function client(data,labels,createInspector){
  const {CATEGORIES,GROUPS}=labels,$=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const inspector=createInspector(data,labels);let page=0,printing=false;
  const study=()=>data.studies.find(s=>s.studyId===$('study').value);
  const label=r=>r.operational?'Gangguan operasional':r.status!=='complete'?'Belum selesai':r.wrong?'Tindakan salah':!r.assessed?'Ketepatan belum dinilai':r.correct?(r.category==='negative'?'Berhenti tepat':r.recovery?'Pulih benar':'Aksi awal benar'):'Tujuan belum tercapai';
  function groups(){const map=new Map();for(const r of study().rows){const key=r.group+'/'+r.caseId;if(!map.has(key))map.set(key,[]);map.get(key).push(r);}const q=$('search').value.trim().toLowerCase();return [...map.entries()].filter(([,rr])=>{const r=rr[0];return ($('group').value==='all'||r.group===$('group').value)&&($('category').value==='all'||r.category===$('category').value)&&(!$('wrong-only').checked||rr.some(x=>x.wrong))&&(!q||`${r.caseId} ${r.title} ${GROUPS[r.group]}`.toLowerCase().includes(q));});}
  const filtered=()=>groups().flatMap(([,rr])=>rr);
  function download(name,text,type='application/json'){const u=URL.createObjectURL(new Blob([text],{type})),a=document.createElement('a');a.href=u;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(u),1000);}
  function metric(title,filter,test){return `<tr><th scope="row">${esc(title)}</th>${Object.keys(study().arms).map(a=>{const rr=study().rows.filter(r=>r.arm===a&&filter(r));return `<td>${rr.length?`${rr.filter(test).length} / ${rr.length}`:'—'}</td>`;}).join('')}</tr>`;}
  function renderComparison(){const s=study();$('comparison-title').textContent=s.studyId==='rm1'?'Pemeringkatan DOM':'Aturan target';$('metric-head').innerHTML='<tr><th>Metrik / kelompok</th>'+Object.keys(s.arms).map(a=>`<th>${a} · ${esc(s.arms[a].short)}</th>`).join('')+'</tr>';
    const metrics=s.studyId==='rm1'?[
      ['Target pada input pertama',()=>true,r=>r.ranking?.targetPresent],['Pemulihan benar',()=>true,r=>r.correct&&r.executed],['Tindakan salah',()=>true,r=>r.wrong],['Berhenti tanpa aksi',()=>true,r=>!r.executed&&!r.operational]
    ]:[['Kontrol: aksi awal benar',r=>r.category==='control',r=>r.correct],['Pemulihan biasa · privat: benar',r=>r.category==='ordinary'&&r.group==='platform',r=>r.correct&&r.executed],['Pemulihan biasa · sintetis: benar',r=>r.category==='ordinary'&&r.group!=='platform',r=>r.correct&&r.executed],['Negatif: tindakan salah',r=>r.category==='negative',r=>r.wrong],['Negatif: berhenti',r=>r.category==='negative',r=>!r.executed&&!r.operational],['Label menyesatkan: tindakan salah',r=>r.category==='stress',r=>r.wrong],['Kontrak missing/stale: aksi benar',r=>r.category==='quality',r=>r.correct&&r.executed],['Kontrak missing/stale: berhenti',r=>r.category==='quality',r=>!r.executed&&!r.operational]];
    $('metrics').innerHTML=metrics.map(args=>metric(...args)).join('');$('arms').innerHTML=Object.entries(s.arms).map(([a,m])=>`<p><b>${a} · ${esc(m.title)}</b><br>${esc(m.description)}</p>`).join('');
    $('resources').innerHTML=Object.keys(s.arms).map(a=>{const rr=s.rows.filter(r=>r.arm===a),known=rr.every(r=>!r.usage.unknown),n=k=>rr.reduce((sum,r)=>sum+(r.usage[k]??0),0),ts=rr.map(r=>r.totalMs).filter(Number.isFinite).sort((a,b)=>a-b),median=ts.length>0&&ts.length===rr.length?(ts[Math.floor((ts.length-1)/2)]+ts[Math.floor(ts.length/2)])/2/1000:null;return `<p><b>${a}</b> · input ${known?n('input'):'unknown'} token · output ${known?n('output'):'unknown'} token · biaya ${known&&s.price?'USD '+((n('input')*s.price.inputUsdPerMillion+n('output')*s.price.outputUsdPerMillion)/1e6).toFixed(8):'unknown'} · median ${median==null?'unknown':median.toFixed(3)+' s'}</p>`;}).join('')+'<p>Tarif tersimpan, bukan invoice. Waktu termasuk timeout dan instrumentasi. Biaya hanya phase yang ditampilkan.</p>';
  }
  function render(){const s=study(),gg=groups(),rr=filtered(),arms=Object.keys(s.arms),pages=Math.max(1,Math.ceil(gg.length/12));page=Math.min(page,pages-1);$('case-head').innerHTML='<tr><th>Kasus / tujuan</th><th>Kelompok</th>'+arms.map(a=>`<th>${a} · ${esc(s.arms[a].short)}</th>`).join('')+'</tr>';
    $('case-rows').innerHTML=(printing?gg:gg.slice(page*12,page*12+12)).map(([,rs])=>{const r=rs[0];return `<tr><td><b>${esc(r.caseId)}</b><span class="case-title">${esc(r.title)}</span></td><td>${esc(r.profile?{U:'Kecil / unik',D:'Duplikat / pemilik',L:'Padat'}[r.profile]:CATEGORIES[r.category])}<small>${esc(GROUPS[r.group])}</small></td>${arms.map(a=>`<td><div class="repeat-results">${rs.filter(x=>x.arm===a).sort((a,b)=>a.repeat-b.repeat).map(x=>`<button class="result-chip ${x.wrong?'bad':x.correct?'good':'warn'}" data-row="${esc(x.id)}" aria-label="${esc(`${x.caseId} ${a} ulangan ${x.repeat}: ${label(x)}. Lihat proses`)}" title="${esc(label(x))}">${x.repeat} <b>${x.wrong?'!':!x.assessed?'?':x.correct?'✓':x.executed?'?':'—'}</b></button>`).join('')||'Slot tidak tersedia'}</div></td>`).join('')}</tr>`;}).join('')||`<tr><td colspan="${arms.length+2}">Tidak ada kasus yang cocok dengan filter.</td></tr>`;
    $('case-count').textContent=`${gg.length} kondisi · ${rr.length} eksekusi sesuai filter`;$('page-number').textContent=`${page+1} / ${pages}`;$('previous').disabled=page===0;$('next').disabled=page===pages-1;
    $('stats').innerHTML=[['Eksekusi studi',s.rows.length],['Kondisi',new Set(s.rows.map(r=>r.caseId)).size],['Tindakan salah',s.rows.filter(r=>r.wrong).length],['Berhenti tanpa aksi',s.rows.filter(r=>!r.executed&&!r.operational&&r.status==='complete').length]].map(([k,v])=>`<div><span>${k}</span><strong>${v}</strong></div>`).join('');
    $('case-rows').querySelectorAll('[data-row]').forEach(b=>b.onclick=()=>{const r=s.rows.find(r=>r.id===b.dataset.row);inspector.open(r.group+'/'+r.caseId,{arm:r.arm,repeat:r.repeat,stage:'context'});});
  }
  function selectStudy(){const s=study(),historical=s.evidenceKind.startsWith('historical');page=0;$('search').value='';$('category').innerHTML='<option value="all">Semua kelompok</option>'+[...new Set(s.rows.map(r=>r.category))].map(k=>`<option value="${k}">${esc(CATEGORIES[k])}</option>`).join('');$('group').value='all';$('wrong-only').checked=false;
    $('mode').textContent=historical?`Arsip ${s.studyId==='rm1'?'D33':'D30'} · ${s.evaluationDate}`:s.evidenceKind==='replay'?'Replay · keputusan model tersimpan':'Demo live · inferensi model baru';
    $('scope').textContent=s.studyId==='rm1'?'12 kondisi × 3 strategi × 3 pengulangan; 108 slot main direncanakan. Kontrak dimatikan.':historical?'D30: 42 kondisi × 3 konfigurasi × 3 pengulangan; 378 slot direncanakan. Sumber yang belum dimuat tetap dinyatakan.':'Hasil run peragaan ini; replay bukan pengukuran performa AI baru.';
    const notices=[...(data.coverage??[]),...(s.coverage??[])];if(s.privateAudit)notices.push('Audit privat lokal aktif; ekspor ringkasan tidak menyertakan diagnostik privat.');if(s.evidenceKind==='replay')notices.push('Browser dijalankan ulang dengan jawaban model tersimpan.');if(s.replayMismatches?.length)notices.push('Replay berbeda: '+s.replayMismatches.join(', '));$('coverage').innerHTML=notices.map(n=>`<p class="notice">${esc(n)}</p>`).join('');
    $('limitations').textContent=s.studyId==='rm1'?'Membandingkan tiga strategi pada representasi dan batas input yang sama. Belum menguji ranking vs tanpa ranking atau manfaat cleansing tersendiri. Kesimpulan berlaku pada kondisi yang tersedia.':'Aturan membantu pada negatif terpilih, tetapi tetap gagal pada label menyesatkan dan dapat menghentikan aksi saat kontrak missing/stale. Bukan diagnosis otomatis bug.';
    $('provenance').textContent=JSON.stringify({evidenceKind:s.evidenceKind,evaluationDate:s.evaluationDate,sources:s.sources,price:s.price},null,2);$('study-label').hidden=!historical;$('view-comparison').hidden=!historical;render();renderComparison();
  }
  $('study').onchange=selectStudy;for(const id of ['search','category','group','wrong-only'])$(id).addEventListener(id==='search'?'input':'change',()=>{page=0;render();});$('previous').onclick=()=>{page--;render();};$('next').onclick=()=>{page++;render();};
  for(const v of ['results','comparison'])$('view-'+v).onclick=()=>{$('cases').hidden=v!=='results';$('comparison').hidden=v!=='comparison';for(const k of ['results','comparison'])$('view-'+k).setAttribute('aria-pressed',String(k===v));};
  $('download').onclick=()=>{const s=study(),selected=filtered();download('self-healing-results.json',JSON.stringify({schemaVersion:1,evidenceKind:s.evidenceKind,studyId:s.studyId,createdAt:data.createdAt,evaluationDate:s.evaluationDate,price:s.price,selection:{group:$('group').value,category:$('category').value,search:$('search').value,wrongCases:$('wrong-only').checked,sourceSlots:s.rows.length},coverage:[...(s.coverage??[]),...(selected.length<s.rows.length?[`Snapshot terfilter: ${selected.length} dari ${s.rows.length} slot sumber.`]:[])],sources:s.sources,privateLoaded:s.privateLoaded,privateAudit:false,rows:selected.map(r=>{if(!r.audit?.privateHost)return r;const {audit,...safe}=r;return safe;})},null,2));};
  $('csv').onclick=()=>{const fields=['studyId','caseId','arm','repeat','group','category','correct','wrong','executed','status'],cell=x=>'"'+String(x??'').replace(/^[=+@-]/,"'$&").replaceAll('"','""')+'"';download('self-healing-filtered.csv',[fields,...filtered().map(r=>fields.map(k=>r[k]))].map(row=>row.map(cell).join(',')).join('\n'),'text/csv');};
  $('print').onclick=()=>window.print();window.addEventListener('beforeprint',()=>{printing=true;render();});window.addEventListener('afterprint',()=>{printing=false;render();});$('generated').textContent='Dibuat '+new Date(data.createdAt).toLocaleString('id-ID');selectStudy();
}

const POLISH=`
body{font:15px/1.6 system-ui,sans-serif;background:#f7f8f5}.rail{position:static;width:auto;height:auto;max-width:1280px;margin:auto;padding:18px 32px;background:transparent;color:var(--ink);display:flex;flex-direction:row;justify-content:space-between;border-bottom:1px solid var(--line);font-size:13px}.rail>span{color:var(--muted);font-size:12px}main{max-width:1280px;margin:auto;padding:24px 32px}h1{font:600 29px/1.3 system-ui,sans-serif;margin:6px 0 18px;letter-spacing:-.5px}h2{font:600 23px/1.4 system-ui,sans-serif}.topline{align-items:center}.report-navigation{display:flex;justify-content:space-between;align-items:end;gap:16px;margin-bottom:14px;flex-wrap:wrap}.report-navigation>label{min-width:260px}button,input,select{min-height:44px}button[aria-pressed=true]{background:var(--ink);color:white}.stats{margin:0 0 22px;padding:12px 0 20px}.stats strong{font:600 27px system-ui,sans-serif}.stats span{font-size:12px;letter-spacing:0}.filters{margin:0;align-items:end}.filters label{font-size:12px}.check{min-height:44px}.check input{min-height:20px}.section-heading{margin:14px 0}.caption{font-size:13px}.case-title{display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;font-size:13px;color:var(--muted);max-width:360px}.cases td:first-child{min-width:220px}.cases small{display:block;color:var(--muted);font-size:11px}.repeat-results{display:flex;gap:4px}.result-chip{padding:8px;min-width:43px;white-space:nowrap;font-size:13px}.result-chip.bad{color:#9c3024;background:#fff1ed;border-color:#dcaca2}.result-chip.good{color:#216653}.result-chip.warn{color:#755d23}.provenance,section>details{margin:18px 0;padding:14px 0;border-top:1px solid var(--line)}#arms{display:block}#arms p{margin:15px 0}pre,code{overflow-wrap:anywhere;white-space:pre-wrap}pre{max-height:450px;overflow:auto}th,td{padding:15px;border-bottom:1px solid var(--line);text-align:left}th{font-size:13px}table{width:100%;border-collapse:collapse}.inspect-shell{max-width:1280px;padding:0}.inspect-heading h1,.run-verdict strong,.stage-title h2{font-family:system-ui,sans-serif}.inspect-heading h1{font-size:24px}.run-verdict strong{font-size:22px}.inspect-heading{padding:20px 0 12px}.run-toolbar{padding:8px 0;gap:14px}.method-explainer{margin:4px 0 16px}.stage-panel{min-height:330px;padding:24px}.stage-title{padding-bottom:12px;margin-bottom:16px}.stage-title h2{font-size:22px}.audit-note{background:transparent;border-left:2px solid #adbbaf;padding:7px 13px;margin:12px 0}.audit-code{max-height:290px}.audit-facts.compact{grid-template-columns:repeat(2,1fr)}.run-verdict{margin-bottom:8px}.locator-pair{margin:8px 0 20px}.paired-block:first-child>h3{margin-top:0}.evidence-details{margin:20px 0}.paired-block+.paired-block{border-top:1px solid var(--line);padding-top:15px;margin-top:20px}.locator-pair{display:flex;gap:12px;flex-wrap:wrap;font-size:12px;overflow-wrap:anywhere}.target-proof{border-left:3px solid var(--teal);padding-left:14px}.stage-nav b{font-size:13px}.stage-nav small,.inspect-top>span{font-size:11px}.audit-note,.audit-muted{font-size:13px}@media(max-width:760px){.cases td:first-child,.cases th:first-child{position:sticky;left:0;z-index:1;background:#f7f8f5;min-width:105px;max-width:105px}.cases .case-title{max-width:105px;-webkit-line-clamp:3}main{padding:20px 16px}.rail{padding:16px;display:flex}.rail>span{display:none}.topline{align-items:start;flex-direction:column}.report-navigation{align-items:stretch;flex-direction:column}h1{font-size:25px}.stats{grid-template-columns:repeat(2,1fr);gap:12px}.filters>label{flex:1;min-width:140px}.filters .check{min-width:100%}.section-heading{flex-direction:row}.inspect-shell{padding:0}.inspect-top{flex-wrap:wrap;gap:8px}.inspect-top>span{order:3;width:100%}.inspect-heading{align-items:stretch;flex-direction:column;gap:12px}.case-select{width:100%;min-width:0}.method-switch{width:100%}.method-switch button{padding:9px;flex:1;gap:6px}.method-switch span{font-size:11px}.run-verdict{padding:15px;flex-direction:column;align-items:start;gap:12px}.run-verdict dl{flex-wrap:wrap;gap:15px}.inspection-layout{display:block}.stage-nav{position:static;display:flex;overflow:auto;margin-bottom:15px}.stage-nav button{flex-shrink:0;padding:10px}.stage-index{display:none}.stage-panel{padding:16px}.audit-facts{grid-template-columns:1fr}.audit-funnel{grid-template-columns:repeat(2,1fr)}.audit-actions{flex-wrap:wrap}.inspect-footer{align-items:start;flex-direction:column}.run-toolbar>label{flex:1}.audit-code,.audit-raw pre{padding:12px;font-size:12px}}`;
