/** Embedded in a standalone HTML report. No network or application execution. */
export function inspectorClient(data,{ARMS,CATEGORIES,GROUPS}){
  const root=document.getElementById('inspector-root');
  const overview=document.getElementById('overview-view'),rail=document.querySelector('.rail');
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const pretty=x=>JSON.stringify(x,null,2),yes=x=>x===true?'Ya':x===false?'Tidak':'Tidak direkam';
  const json=(x,title='Data yang direkam')=>`<details class="audit-raw"><summary>${esc(title)}</summary><pre>${esc(pretty(x))}</pre></details>`;
  const code=x=>`<pre class="audit-code">${esc(x)}</pre>`;
  const note=(text,tone='')=>`<p class="audit-note ${tone}">${esc(text)}</p>`;
  const fact=(k,v)=>`<div><dt>${esc(k)}</dt><dd>${esc(v??'Tidak direkam')}</dd></div>`;
  const reasons={
    'original-success':'Locator awal bekerja. Tidak perlu mencari pengganti atau memanggil AI.',
    recovered:'Aksi dengan locator pengganti selesai. Ketepatan sasaran tetap dinilai secara terpisah.',
    abstained:'AI tidak memilih locator; pemulihan berhenti tanpa menjalankan kandidat.',
    'spec-unknown':'Bukti belum cukup untuk mengizinkan kandidat sesuai aturan requirement.',
    'spec-refused':'Acuan requirement secara eksplisit menolak pemulihan ini.',
    spec_insufficient_evidence:'Ada syarat identitas kandidat yang tidak ditemukan pada bukti elemen.',
    spec_evidence_matched:'Semua syarat bukti yang dideklarasikan cocok. Ini belum menjamin fungsi bisnisnya benar.',
    spec_retired:'Fitur ditandai nonaktif pada aturan yang disuplai.',
    spec_missing:'Aturan requirement tidak tersedia. Ini bukan bukti bahwa fiturnya dihapus.',
    spec_revision_mismatch:'Versi aturan tidak cocok dengan versi yang diharapkan.',
    spec_action_mismatch:'Jenis aksi tidak cocok dengan aturan.',
    spec_target_unavailable:'Elemen tidak lagi tersedia ketika akan diperiksa.',
    ambiguous:'Locator menunjuk lebih dari satu elemen.',no_match:'Locator tidak menemukan elemen.',
    not_visible:'Elemen tidak terlihat.',not_enabled:'Elemen tidak aktif.',action_mismatch:'Elemen tidak cocok dengan jenis aksi.',
    selector_validation_failed:'Pemeriksaan locator gagal.',no_candidate:'Tidak ada kandidat yang dipilih.',
    'attempt-limit':'Batas percobaan tercapai.', 'time-limit':'Batas waktu pemulihan tercapai.',
    'provider-failure':'Panggilan penyedia AI gagal; pemulihan tidak boleh dianggap berhasil.',
    'context-failure':'Pengumpulan konteks gagal.',nonrecoverable:'Kegagalan awal berada di luar pemulihan locator yang didukung.',
  };
  const explain=x=>reasons[x]??(x?`Kode yang direkam: ${x}`:'Tidak ada alasan kegagalan yang direkam.');
  const stages=[['start','Aksi awal'],['context','DOM & ranking'],['request','Input ke AI'],['response','Output AI'],['validation','Validasi locator'],['rules','Aturan requirement'],['outcome','Bukti hasil'],['source','Sumber & batas']];
  const groups=new Map();for(const row of data.rows){const key=row.group+'/'+row.caseId;if(!groups.has(key))groups.set(key,[]);groups.get(key).push(row);}
  const keys=[...groups.keys()];let selectedKey=keys[0],arm='C',repeat=1,attemptNumber=null,stage='start',returnFocus=null;
  const currentRows=()=>groups.get(selectedKey)??[];
  const selectedRow=()=>currentRows().find(r=>r.arm===arm&&r.repeat===repeat);
  function download(filename,text,type='application/json'){
    const url=URL.createObjectURL(new Blob([text],{type}));const a=document.createElement('a');a.href=url;a.download=filename;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);
  }
  function close(){root.hidden=true;overview.hidden=false;rail.style.display='';document.body.classList.remove('inspecting');returnFocus?.focus();}
  function render(focusId){
    const row=selectedRow(),fallback=currentRows()[0];
    const audit=row?.audit,e=audit?.event,attempts=audit?.attempts??[],attempt=attempts.find(a=>a.number===attemptNumber)??attempts[0];
    attemptNumber=attempt?.number??null;
    let request=null,input=null;
    try{request=JSON.parse(attempt?.request?.body??'null');const message=request?.messages?.find(m=>m.role==='user');input=message?JSON.parse(message.content):null;}catch{/* Raw content remains readable even when user text is not JSON. */}
    const observed=attempt?.inputContext??audit?.initialContext,coverage=attempt?.inputCoverage??observed?.coverage??input?.coverage;
    const spec=input?.targetSpec??observed?.targetSpec,decision=attempt?.specDecision;
    const outcome=audit?.outcome;
    const native=e?.stopReason==='original-success',called=attempt?.providerCalled===true;
    function status(id){
      if(!row)return 'Slot tidak tersedia';
      if(!audit)return 'Bukti belum dimuat';
      if(['start','outcome','source'].includes(id))return 'Terekam';
      if(native)return 'Dilewati';
      if(id==='context')return observed?'Terekam':'Tidak direkam';
      if(id==='request')return attempt?.request?.body?(attempt.request.status==='redacted'?'Salinan direduksi':attempt.request.integrity==='verified'?'Hash cocok':'Periksa integritas'):called?'Tidak direkam':'Tidak dipanggil';
      if(id==='response')return !called?'Tidak dipanggil':attempt?.response?.text?'Terekam':'Hasil parsing';
      if(id==='validation')return attempt?.candidateAccepted?'Lolos teknis':attempt?.validations?.length?'Ditolak':'Belum lolos';
      if(id==='rules')return arm!=='C'?'Tidak diberlakukan':decision?(decision.outcome==='accepted'?'Diizinkan':'Dihentikan'):'Belum tercapai';
    }
    const result=!row?'Slot tidak tersedia':row.operational?'Gangguan operasional':row.wrong?'Efek salah tercatat':row.category==='quality'&&!row.executed?'Berhenti: acuan tidak cukup':row.correct?(row.category==='negative'?'Berhenti dengan tepat':row.recovery?'Pulih ke sasaran benar':'Aksi awal benar'):'Tujuan belum tercapai';
    const methodCopy={A:'AI menerima tujuan tes, locator lama dan konteks DOM. Tidak ada aturan requirement tambahan. Validasi locator dasar tetap berjalan.',B:'Aturan requirement ikut masuk ke pesan untuk AI. AI diminta mempertimbangkannya; kode belum memaksakan pemeriksaan aturan sebelum aksi.',C:'Input requirement ke AI sama dengan B. Setelah usulan lolos validasi locator, kode memeriksa bukti elemen terhadap aturan sebelum menjalankan aksi.'}[arm];
    root.innerHTML=`<div class="inspect-shell">
      <div class="inspect-top"><button id="audit-back">← Semua hasil</button><span>${audit?.privateHost?'AUDIT LOKAL · BUKTI APLIKASI PRIVAT':data.evidenceKind==='replay'?'REPLAY · KEPUTUSAN AI TERSIMPAN':data.evidenceKind==='live-demo'?'DEMO LIVE · PANGGILAN BARU':'BUKTI TERSIMPAN · D30'}</span><button id="audit-download" ${audit?'':'disabled'}>Unduh bukti run</button></div>
      <div class="inspect-heading"><div><p class="eyebrow">${esc(GROUPS[fallback.group])} / ${esc(CATEGORIES[fallback.category])}</p><h1><span>${esc(fallback.caseId)}</span>${esc(fallback.title)}</h1></div>
      <label class="case-select">Pindah kasus<select id="audit-case">${keys.map(k=>{const r=groups.get(k)[0];return `<option value="${esc(k)}" ${k===selectedKey?'selected':''}>${esc(r.caseId+' · '+r.title)}</option>`;}).join('')}</select></label></div>
      <div class="run-toolbar"><div class="method-switch" role="group" aria-label="Konfigurasi">${['A','B','C'].map(a=>`<button id="audit-arm-${a}" data-arm="${a}" aria-pressed="${arm===a}"><b>${a}</b><span>${a==='A'?'Tanpa aturan':a==='B'?'Aturan ke AI':'+ Pemeriksaan kode'}</span></button>`).join('')}</div>
      <label>Pengulangan<select id="audit-repeat">${[...new Set(currentRows().map(r=>r.repeat))].sort((a,b)=>a-b).map(n=>`<option value="${n}" ${n===repeat?'selected':''}>Ulangan ${n}</option>`).join('')}</select></label>
      <label>Percobaan healing<select id="audit-attempt" ${attempts.length?'':'disabled'}>${attempts.length?attempts.map(a=>`<option value="${a.number}" ${a.number===attemptNumber?'selected':''}>Attempt ${a.number}</option>`).join(''):'<option>Tidak ada attempt</option>'}</select></label></div>
      <p class="method-explainer">${esc(methodCopy)}</p>
      <div class="run-verdict ${row?.wrong?'is-wrong':row?.correct?'is-correct':'is-unknown'}"><div><small>HASIL RUN INI</small><strong>${esc(result)}</strong><p>${esc(native?'Locator awal berhasil. Tidak ada DOM recovery, request AI, atau pemeriksaan kandidat pengganti.':e?explain(e.stopReason):'Buka bukti hasil untuk melihat penilaian yang tersedia.')}</p></div><dl>${fact('API calls',row?.usage?.requests??'—')}${fact('Durasi langkah',row?.totalMs==null?'—':(row.totalMs/1000).toFixed(2)+' s')}${fact('Aksi dijalankan',row?yes(row.executed):'—')}</dl></div>
      <div class="inspection-layout"><nav class="stage-nav" aria-label="Tahap eksekusi">${stages.map(([id,title],i)=>`<button id="stage-${id}" data-stage="${id}" aria-current="${stage===id?'step':'false'}"><span class="stage-index">${String(i+1).padStart(2,'0')}</span><span><b>${title}</b><small>${esc(status(id))}</small></span></button>`).join('')}</nav>
      <section class="stage-panel" aria-labelledby="stage-title"><div class="stage-title"><span class="eyebrow">${esc(status(stage))}</span><h2 id="stage-title">${stages.find(s=>s[0]===stage)[1]}</h2></div>${content()}</section></div>
      <footer class="inspect-footer"><span>Hasil tes, keputusan pemeriksa, dan usulan AI adalah bukti yang berbeda.</span><button id="audit-next-stage">${stage==='source'?'Kembali ke aksi awal':'Tahap berikutnya →'}</button></footer>
    </div>`;
    function content(){
      if(!row)return note('Eksekusi untuk pilihan ini tidak tersedia. Tidak ada hasil yang diasumsikan.');
      if(!audit)return note('Detail diagnostik belum dimuat. Untuk arsip privat, aktifkan opsi audit lokal saat membuat report. Angka ringkasan tetap berasal dari hasil tersimpan.');
      if(stage==='source')return note('Bukti ini dibaca dari rekaman. Tampilan tidak menjalankan ulang AI atau merekonstruksi tahapan yang tidak direkam.')+json(audit.source,'Identitas sumber')+json(audit.config,'Konfigurasi yang direkam')+`<ul class="audit-limits">${(audit.missing??[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`+note('Respons model tidak berisi penjelasan pemikiran internal. Alasan penolakan yang ditampilkan berasal dari validator dan aturan, bukan alasan yang dikarang atas nama AI.');
      if(stage==='start')return `<p class="stage-intro">Apa yang hendak dilakukan test sebelum proses self-healing.</p><dl class="audit-facts">${fact('Tujuan tes',e?.task?.description)}${fact('Jenis aksi',e?.action)}${fact('Locator awal',e?.originalSelector)}${fact('Pemulihan dipicu',yes(e?.recoveryTriggered))}${fact('Kegagalan awal',e?.originalFailure?`${e.originalFailure.name} · ${e.originalFailure.classification}`:'Tidak ada kegagalan yang direkam')}</dl>`+note(native?'Aksi asli sudah berhasil. Jadi hasil ini tidak membuktikan kemampuan AI memperbaiki locator; ini kontrol untuk memastikan perilaku normal tetap berjalan.':explain(e?.stopReason))+json(e,'Event runtime lengkap yang tersedia');
      if(stage==='outcome'){
        const checks=audit.privateHost?[
          ['Oracle tujuan tes',outcome?.oracleCorrect],['Field/entitas lain tetap sesuai',outcome?.guardUnchanged],['Ada efek salah',row.wrong]
        ]:[['Penilaian semantik',outcome?.assessment?.semantic],['Ada efek salah',outcome?.assessment?.wrongEffect],['Tujuan yang diharapkan',outcome?.assessment?.goalExpected??outcome?.expectation?.goalExpected]];
        const explanation=row.wrong?'Evaluator mencatat efek yang salah. Locator valid atau aksi selesai tidak mengubah hasil ini menjadi benar.':row.correct&&native?'Aksi awal berhasil, dan evaluator menilai tujuan tercapai tanpa efek salah. Tidak ada perbaikan AI pada run ini.':row.correct&&row.category==='negative'?'Untuk kondisi ini, tindakan yang diharapkan adalah tidak mengubah sasaran lain. Berhenti merupakan hasil yang tepat, bukan recovery berhasil.':row.correct?'Evaluator menilai tujuan tes tercapai tanpa efek salah. Ini penilaian sesudah aksi, terpisah dari persetujuan locator.':'Tujuan tes belum tercapai. Lihat riwayat percobaan dan hasil pemeriksaan; berhenti aman tidak sama dengan pemulihan berhasil.';
        return `<p class="stage-intro">${esc(explanation)}</p><div class="proof-grid">${checks.map(([k,v])=>`<article><small>${esc(k)}</small><strong>${esc(typeof v==='boolean'?yes(v):v??'Tidak direkam')}</strong></article>`).join('')}</div>`+note('Bukti evaluator berada di luar input AI. Status “benar” berasal dari pengujian efek aplikasi, bukan pengakuan AI.')+json(outcome,'Hasil / keadaan aplikasi yang direkam')+json(audit.assessments,'Penilaian tiap event / attempt')+(audit.calls?json(audit.calls,'Riwayat aksi dan efek yang diamati'):'')+note('Rekaman tidak selalu memuat nilai expected, before, dan after secara lengkap. Flag oracle adalah hasil pemeriksa yang direkam; jangan menganggapnya sebagai snapshot yang tidak tersedia.');
      }
      if(native)return note('Tahap ini dilewati: locator awal berhasil. Tidak ada proses healing, request AI, atau kandidat pengganti pada run ini.','skipped');
      if(stage==='context'){
        if(!observed)return note('Konteks DOM untuk tahap ini tidak direkam atau pengumpulan belum tercapai.');
        const candidates=observed.candidates??[];
        const funnel=[['Dipindai','scanned'],['Tidak eligible','ineligible'],['Kandidat ditemukan','discovered'],['Masuk konteks','included']];
        return `<p class="stage-intro">Elemen disaring sesuai aksi, informasinya dibersihkan, lalu kandidat diberi skor dan dibatasi sebelum dikirim ke AI.</p>`+note(attempt?.inputContext?'Ini konteks yang direkam untuk attempt terpilih. Payload yang benar-benar dikirim tersedia di Input ke AI.':'Ini konteks awal event; konteks khusus attempt tidak tersedia.')+`<div class="audit-funnel">${funnel.map(([k,v])=>`<article><strong>${esc(coverage?.[v]??'—')}</strong><span>${k}</span></article>`).join('')}</div>`+json(coverage,'Seluruh hitungan, batas karakter dan pemotongan')+`<h3>Urutan kandidat yang terekam <small>${candidates.length} kandidat</small></h3><p class="audit-muted">Skor di bawah adalah hasil ranking yang disimpan, bukan skor yang dihitung ulang oleh report. Rincian bobot per fitur tidak direkam.</p><div class="candidate-list">${candidates.map((c,i)=>`<details><summary><span class="candidate-rank">${i+1}</span><span class="candidate-name">${esc(c.label||c.features?.text||c.features?.name||c.tag)}<small>${esc(c.selector||'Tidak ada locator terverifikasi')}</small></span><span class="candidate-score">${esc(c.score)}<small>skor</small></span></summary>${code(c.selector??'')}${json(c,'Fitur, owner, duplikasi dan usulan locator')}</details>`).join('')}</div><h3>HTML hasil cleansing / tambahan</h3>`+(typeof observed.cleanedDom==='string'?code(observed.cleanedDom):note('Tidak ada HTML tambahan pada konteks ini. Informasi DOM dikirim sebagai daftar kandidat dan atribut yang sudah dipilih; bukan berarti proses pemilihan/pembersihan DOM tidak berjalan.'))+note('DOM mentah sebelum cleansing dan identitas setiap elemen yang dibuang tidak disimpan pada arsip ini.');
      }
      if(stage==='request'){
        if(!called)return note('Tidak ada pemanggilan provider pada attempt ini.');
        if(!attempt.request?.body)return note(`Body request tidak tersedia untuk ditampilkan. Status: ${attempt.request?.status??'missing'}.`)+json(attempt.request,'Identitas request yang tersedia');
        const p=attempt.request;
        return `<p class="stage-intro">Inilah request yang direkam pada batas pemanggilan provider. System prompt berisi instruksi; pesan user memuat konteks tugas dan halaman.</p>`+note(p.status==='redacted'?'Salinan tampilan mengandung redaksi secret. Ini bukan salinan byte-identik.':p.integrity==='verified'?'SHA-256 body cocok dengan fingerprint request dan attempt yang tersedia. Tidak direkonstruksi dari prompt saat ini.':'Integritas request belum terverifikasi atau berbeda dari fingerprint yang direkam.','integrity-'+p.integrity)+`<dl class="audit-facts compact">${fact('Model diminta',request?.model)}${fact('Temperature',request?.temperature)}${fact('Batas output',request?.max_tokens)}${fact('Ukuran body',p.bytes+' bytes')}</dl><div class="audit-actions"><button id="audit-request-download">Unduh body request</button><button id="audit-request-copy">Salin request</button></div>`+request.messages.map((m,i)=>`<h3>${m.role==='system'?'System prompt':m.role==='user'?'Pesan user / konteks':'Pesan '+m.role}<small>message ${i+1}</small></h3>${code(m.content)}`).join('')+json(p,'Hash dan status integritas')+note(arm==='A'?'Tidak ada targetSpec tambahan pada metode A.': 'Aturan requirement berada pada targetSpec di pesan user. B dan C memberikan aturan tersebut sebagai konteks; hanya C yang menegakkannya lewat kode sebelum aksi.');
      }
      if(stage==='response'){
        if(!called)return note('Provider tidak dipanggil pada attempt ini.');
        const raw=attempt.response?.text;
        return `<p class="stage-intro">Apa yang dikembalikan provider, lalu locator apa yang dicatat setelah parsing.</p>`+note(attempt.response?.status==='replayed'?'Output ini berasal dari keputusan yang diputar ulang, bukan panggilan AI baru.':raw?'Teks output provider tersimpan.':'Respons mentah tidak direkam di arsip ini. Locator di bawah adalah hasil parsing; report tidak mengarang JSON respons asli.')+(raw?`<h3>Output provider</h3>${code(raw)}`:'')+`<h3>Locator hasil parsing</h3>${code(attempt.proposedSelector===null?'null — tidak ada locator yang dipilih':attempt.proposedSelector??'Tidak direkam')}<dl class="audit-facts compact">${fact('Model yang dikembalikan',attempt.providerMetadata?.returnedModel)}${fact('Finish reason',attempt.providerMetadata?.finishReason)}${fact('Input tokens',attempt.usage?.inputTokens)}${fact('Output tokens',attempt.usage?.outputTokens)}</dl>`+json(attempt.observationRefresh??null,'Observasi ulang setelah tidak ada pilihan')+note('Format respons meminta selector atau null. Tidak ada alasan naratif AI yang bisa diaudit jika model tidak mengeluarkan alasan tersebut.');
      }
      if(stage==='validation'){
        return `<p class="stage-intro">Pemeriksaan dasar memastikan usulan bisa dipakai pada elemen yang tepat secara struktur: satu target, terlihat, aktif, dan cocok dengan aksi.</p><dl class="audit-facts">${fact('Usulan awal',attempt?.proposedSelector)}${fact('Locator yang diterima',attempt?.selector)}${fact('Lolos validasi dasar',yes(attempt?.candidateAccepted))}${fact('Aksi recovery dijalankan',yes(attempt?.actionExecuted))}</dl>`+note(attempt?.candidateAccepted?'Locator lolos pemeriksaan teknis. Pada C, aturan requirement masih harus diperiksa sebelum aksi. Ini belum berarti sasaran bisnis benar.':explain(attempt?.reason))+json(attempt?.validations??null,'Varian locator yang ditolak / alasan / jumlah match')+note('Daftar varian bisa kosong jika usulan pertama lolos. Arsip hanya merekam keputusan agregat untuk pemeriksaan yang berhasil; tidak mengarang log per-check.')+json(input?.feedback??null,'Umpan balik penolakan yang masuk ke AI pada attempt ini');
      }
      if(stage==='rules'){
        if(arm==='A')return note('Metode A tidak diberi aturan requirement tambahan dan tidak menjalankan pemeriksa aturan. Validasi locator dasar tetap ada.');
        const contract=spec?.contract;
        let html=`<p class="stage-intro">${arm==='B'?'Aturan ini diberikan kepada AI sebagai konteks. Tidak ada keputusan pemeriksa kode yang diberlakukan pada B.':'Kode membandingkan bukti elemen terpilih dengan setiap syarat yang dideklarasikan sebelum klik / fill.'}</p>`;
        html+=`<dl class="audit-facts">${fact('Tujuan requirement',contract?.intent)}${fact('ID / revisi',contract?contract.requirementId+' / '+contract.revision:e?.targetSpec?.requirementId)}${fact('Status fitur',contract?.status)}${fact('Aksi yang diizinkan',contract?.action)}${fact('Kesesuaian acuan',spec?.applicability??e?.targetSpec?.applicability)}</dl>`;
        if(arm==='C')html+=note(decision?explain(decision.reason):'Pemeriksa aturan belum tercapai pada attempt ini. Misalnya AI tidak memilih kandidat, atau validasi dasar belum lolos.');
        html+=`<div class="rule-list">${(contract?.allOf??[]).map((rule,i)=>{const result=decision?.clauses?.find(c=>c.index===i);return `<article><div class="rule-heading"><h3>Syarat ${i+1}</h3><span class="pill ${result?.matched===true?'good':result?.matched===false?'bad':'warn'}">${result?result.matched?'Cocok':'Belum cocok':arm==='B'?'Konteks AI':'Belum diperiksa'}</span></div><p>Cari salah satu frasa: <b>${esc(rule.anyOf.join(' · '))}</b></p><p>Pada: ${esc(rule.sources.join(', '))}</p>${result?`<p>Sumber yang cocok: ${esc(result.source??'Tidak ditemukan')}</p>`:''}${decision?.observed?json(Object.fromEntries(rule.sources.map(s=>[s,decision.observed[s]??null])),'Bukti elemen untuk syarat ini'):''}</article>`;}).join('')}</div>`;
        return html+json(spec??e?.targetSpec,'Aturan, versi dan provenance yang tersedia')+json(decision??null,'Keputusan pemeriksa pada attempt terpilih')+note('Pemeriksa ini mencocokkan frasa pada bukti label/atribut/konteks. Ia tidak membuktikan seluruh perilaku aplikasi dan dapat tertipu label yang menyesatkan.');
      }
      return '';
    }
    const bind=(id,fn)=>{const el=document.getElementById(id);if(el)el.onclick=fn;};
    bind('audit-back',close);bind('audit-download',()=>download(`${fallback.caseId}-${arm}-r${repeat}-audit.json`,pretty({caseId:fallback.caseId,arm,repeat,audit})));
    root.querySelectorAll('[data-arm]').forEach(b=>b.onclick=()=>{arm=b.dataset.arm;attemptNumber=null;render(b.id);});
    root.querySelectorAll('[data-stage]').forEach(b=>b.onclick=()=>{stage=b.dataset.stage;render(b.id);});
    document.getElementById('audit-case').onchange=e=>{selectedKey=e.target.value;repeat=1;attemptNumber=null;render('audit-case');};
    document.getElementById('audit-repeat').onchange=e=>{repeat=Number(e.target.value);attemptNumber=null;render('audit-repeat');};
    document.getElementById('audit-attempt').onchange=e=>{attemptNumber=Number(e.target.value);render('audit-attempt');};
    bind('audit-next-stage',()=>{stage=stages[(stages.findIndex(x=>x[0]===stage)+1)%stages.length][0];render('stage-'+stage);});
    bind('audit-request-download',()=>download(`${fallback.caseId}-${arm}-r${repeat}-attempt${attemptNumber}-request.json`,attempt.request.body));
    bind('audit-request-copy',async()=>{const button=document.getElementById('audit-request-copy');try{await navigator.clipboard.writeText(attempt.request.body);button.textContent='Tersalin';}catch{button.textContent='Gunakan unduh request';}});
    if(focusId)document.getElementById(focusId)?.focus();
  }
  document.addEventListener('keydown',event=>{if(event.key==='Escape'&&!root.hidden)close();});
  return {open(key){if(!groups.has(key))return;returnFocus=document.activeElement;selectedKey=key;arm='C';repeat=1;attemptNumber=null;stage='start';overview.hidden=true;rail.style.display='none';root.hidden=false;document.body.classList.add('inspecting');render();window.scrollTo(0,0);document.getElementById('audit-back').focus();}};
}

export const INSPECTOR_CSS=`
[hidden]{display:none!important}.inspecting{background:#f5f5f0}.inspect-shell{max-width:1450px;margin:auto;padding:25px 44px 0}.inspect-top{display:flex;justify-content:space-between;align-items:center;gap:20px;padding-bottom:22px;border-bottom:1px solid var(--line)}.inspect-top>span{font-size:10px;letter-spacing:1.3px;color:var(--muted)}.inspect-top button{background:#fffef9}.inspect-heading{display:flex;justify-content:space-between;align-items:end;gap:40px;padding:27px 0 20px}.inspect-heading h1{font:31px/1.3 Georgia,serif;letter-spacing:-.5px;margin:8px 0;max-width:760px}.inspect-heading h1>span{display:block;color:var(--teal);font:13px/1.7 'SFMono-Regular',Consolas,monospace;letter-spacing:0}.case-select{width:290px;min-width:210px}.case-select select{width:100%}.run-toolbar{display:flex;align-items:end;gap:20px;flex-wrap:wrap;padding:18px 0 0}.method-switch{display:flex;gap:6px;margin-right:auto}.method-switch button{display:flex;align-items:center;gap:12px;padding:12px 17px;min-height:53px;background:#fffef9;border-color:var(--line);text-align:left}.method-switch b{font:21px Georgia,serif}.method-switch button[aria-pressed=true]{background:var(--ink);border-color:var(--ink);color:white}.method-switch span{font-size:11px}.method-explainer{font-size:12px;color:var(--muted);margin:15px 0 20px;max-width:840px;line-height:1.65}.run-verdict{display:flex;justify-content:space-between;align-items:center;gap:30px;padding:21px 25px;background:#fffef9;border:1px solid var(--line);border-left:4px solid #a08b47;margin-bottom:29px}.run-verdict.is-correct{border-left-color:var(--teal)}.run-verdict.is-wrong{border-left-color:var(--red)}.run-verdict small{font-size:9px;letter-spacing:1.3px;color:var(--muted)}.run-verdict strong{display:block;font:24px/1.4 Georgia,serif}.run-verdict p{font-size:12px;color:var(--muted);margin:5px 0 0;max-width:630px}.run-verdict dl{display:flex;gap:28px;margin:0;flex-shrink:0}.run-verdict dt{font-size:10px;margin:0}.run-verdict dd{font-size:14px;margin:6px 0 0}.inspection-layout{display:grid;grid-template-columns:218px minmax(0,1fr);gap:35px;align-items:start}.stage-nav{position:sticky;top:24px;display:grid;gap:5px}.stage-nav button{display:flex;align-items:center;gap:16px;text-align:left;border:1px solid transparent;padding:13px 12px;background:transparent;border-radius:5px}.stage-index{font:11px 'SFMono-Regular',Consolas,monospace;color:#839995}.stage-nav b{font-size:12px;font-weight:600;display:block}.stage-nav small{display:block;font-size:10px;color:var(--muted);margin-top:4px}.stage-nav button[aria-current=step]{background:#e2ece4;border-color:#c6d8cc}.stage-panel{background:#fffefb;border:1px solid #d9e0d8;min-height:510px;padding:30px 34px;border-radius:8px;min-width:0}.stage-title{border-bottom:1px solid var(--line);padding-bottom:20px;margin-bottom:22px}.stage-title h2{font-size:28px;margin-top:6px}.stage-intro{font-size:14px;line-height:1.8;max-width:800px}.audit-note{background:#eef2ec;border-left:3px solid #a1b7a8;color:#415d57;padding:14px 18px;font-size:12px;line-height:1.8;margin:20px 0;overflow-wrap:anywhere}.audit-note.skipped{background:#f1f0e8;border-color:#bcb596}.audit-note.integrity-mismatch{background:#fae9e2;border-color:var(--red)}.audit-code,.audit-raw pre{font:12px/1.75 'SFMono-Regular',Consolas,monospace;white-space:pre-wrap;overflow-wrap:anywhere;tab-size:2;background:#f2f4ed;border:1px solid #e0e6da;border-radius:5px;padding:18px 21px;max-height:510px;overflow:auto;letter-spacing:0}.audit-raw{margin:16px 0;border:1px solid var(--line);border-radius:5px;background:#fffefb;min-width:0}.audit-raw>summary{padding:13px 17px;font-size:12px;color:var(--teal);overflow-wrap:anywhere}.audit-raw pre{margin:0;border:0;border-top:1px solid var(--line);border-radius:0 0 5px 5px}.audit-facts{display:grid;grid-template-columns:1fr 1fr;gap:0 28px;margin:12px 0 20px}.audit-facts>div{padding:12px 0;border-bottom:1px solid #e6e9df;min-width:0}.audit-facts dt{font-size:11px;margin:0}.audit-facts dd{font-size:13px;margin:6px 0 0;line-height:1.7;overflow-wrap:anywhere}.audit-facts.compact{grid-template-columns:repeat(4,1fr)}.audit-muted{font-size:12px;line-height:1.7;color:var(--muted)}.stage-panel h3{font-size:14px;margin-top:25px}.stage-panel h3>small{font-weight:400;color:var(--muted);margin-left:13px;font-size:11px}.audit-funnel{display:grid;grid-template-columns:repeat(4,1fr);gap:15px;margin:22px 0}.audit-funnel article{border-bottom:2px solid #cadacd;padding:10px 0}.audit-funnel strong{display:block;font:29px Georgia,serif}.audit-funnel span{font-size:10px;color:var(--muted)}.candidate-list{border-top:1px solid var(--line)}.candidate-list>details{border-bottom:1px solid var(--line)}.candidate-list>details>summary{display:flex;align-items:center;gap:18px;padding:14px 5px;list-style:none;font-size:12px}.candidate-rank{font:13px Georgia,serif;border:1px solid #cedbd0;border-radius:50%;width:28px;height:28px;display:grid;place-items:center;flex-shrink:0}.candidate-name{flex:1;overflow-wrap:anywhere;min-width:0}.candidate-name small{display:block;font:10px/1.6 'SFMono-Regular',Consolas,monospace;color:var(--muted);margin-top:4px}.candidate-score{font-size:16px}.candidate-score small{display:block;font-size:9px;color:var(--muted)}.audit-actions{display:flex;gap:10px;margin:18px 0}.rule-list article{border:1px solid var(--line);padding:18px 21px;border-radius:5px;margin:15px 0;font-size:12px;overflow-wrap:anywhere}.rule-heading{display:flex;align-items:center;justify-content:space-between;gap:12px}.rule-heading h3{margin:0}.proof-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:18px;margin:25px 0}.proof-grid article{border:1px solid var(--line);padding:17px;border-radius:5px;min-width:0}.proof-grid small{display:block;color:var(--muted);font-size:10px}.proof-grid strong{display:block;font-size:17px;margin-top:9px;overflow-wrap:anywhere}.audit-limits{font-size:12px;color:var(--muted);line-height:1.8;padding-left:20px}.audit-limits li{padding-bottom:12px}.inspect-footer{align-items:center;letter-spacing:0;padding:25px 0 32px;font-size:11px}.inspect-footer button{flex-shrink:0}
@media(max-width:1050px){.inspect-shell{padding-inline:25px}.inspection-layout{grid-template-columns:180px minmax(0,1fr);gap:20px}.stage-panel{padding:24px}.run-verdict{align-items:start;flex-direction:column;gap:17px}.run-verdict dl{flex-wrap:wrap}.inspect-heading{align-items:start;flex-direction:column;gap:15px}.case-select{width:100%;max-width:600px}.method-switch{width:100%}.method-switch button{flex:1}.audit-facts.compact{grid-template-columns:1fr 1fr}}
@media(max-width:700px){.inspect-shell{padding:18px 16px 0}.inspect-top{flex-wrap:wrap;gap:12px}.inspect-top>span{order:3;width:100%;font-size:9px}.inspect-heading h1{font-size:25px}.method-switch{gap:4px}.method-switch button{padding:10px;gap:8px;align-items:start}.method-switch span{font-size:10px}.run-toolbar{gap:14px}.run-toolbar label{flex:1;min-width:0}.run-toolbar select{width:100%}.run-verdict{padding:17px}.run-verdict strong{font-size:22px}.run-verdict dl{gap:17px}.inspection-layout{grid-template-columns:1fr;gap:17px}.stage-nav{position:static;grid-template-columns:repeat(2,minmax(0,1fr));gap:4px}.stage-nav button{padding:9px 8px;gap:10px;border-color:var(--line);min-width:0}.stage-nav b{font-size:10px}.stage-nav small{font-size:9px}.stage-index{font-size:9px}.stage-panel{padding:21px 17px;min-height:350px}.stage-title h2{font-size:24px}.audit-facts{grid-template-columns:1fr}.audit-funnel{gap:9px}.audit-funnel strong{font-size:23px}.audit-funnel span{font-size:9px}.proof-grid{grid-template-columns:1fr;gap:10px}.proof-grid article{padding:14px}.proof-grid strong{font-size:15px}.audit-code,.audit-raw pre{padding:14px;font-size:11px}.inspect-footer{align-items:start;gap:15px}.inspect-footer span{font-size:10px}.audit-actions{flex-wrap:wrap}}
@media print{.inspect-top,.stage-nav,.run-toolbar,.case-select,.inspect-footer{display:none!important}.inspection-layout{display:block}.inspect-shell{padding:0}.stage-panel{border:0;padding:15px 0}.audit-code,.audit-raw pre{max-height:none}.run-verdict{break-inside:avoid}}
`;
