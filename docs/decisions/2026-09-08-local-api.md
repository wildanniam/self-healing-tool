# Local API configuration — 2026-09-08

### D18 — Start from the reference API settings

**Owner instruction:** open the new repository in Cursor, create a local environment file for manual key entry, and match the API configuration to the thesis prototype.

Initial development values: `OPENAI_MODEL=gpt-4o-mini`, `OPENAI_MAX_TOKENS=500`, `OPENAI_TEMPERATURE=0`, `HEALING_MAX_RETRIES=3`, and `HEALING_DOM_MAX_CHARS=8000`. These five non-secret values were verified against the locally available reference. The provider interface is OpenAI Chat Completions.

The credential is entered by the owner into an ignored local `.env`; only the blank-key example is versioned. Existing local content must be preserved. No credential, business configuration, or private auth helper is migrated.

This decision resolves the initial model/profile choice that was open in [the foundation decisions](2026-09-07-foundation.md). It does not freeze the final experiment protocol, authorize unbounded spending, establish source-redistribution rights, or implement runtime validation. Unknown configuration must still be rejected under INT-003. An environment template is not evidence that the provider or recovery code works.

Affected requirements: INT-003, HEAL-002, CTX-004. The corresponding implementation tasks remain unchecked. Setup is tracked by [issue #3](https://github.com/wildanniam/self-healing-tool/issues/3), dependent on foundation PR #2. See [local setup](../local-api-setup.md) for the meanings and limits of these values.

Source: the owner's explicit request dated 2026-09-08 and read-only reference configuration inspection. No change to D01–D17's research, private-host, or public-release boundaries is implied.
