# D31 supervisor presentation workflow

### D31 — Build a readable comparison and runnable demonstration

On 16 September Wildan requested a TA-like manual demo for a meeting with Pak Jati: retain the existing results, provide simple commands for the three configurations, and open an informative HTML comparison. This is standard engineering under issue #21 and branch `codex/21-presentation-demo`, based on the D30 branch. It does not change the research method or authorize a new paid evaluation, merge, release, external message or submission.

Operational choice: default to a standalone report from retained results; add a headed A/B/C replay with exact input checks and an optional explicit live command. Replay offers a repeatable walkthrough without API/network risk and must be labeled as recorded model decisions. The live option uses a fresh small ledger and separate output; it is not executed as part of this increment.

Keep every category, failure, repeat and limitation. Keep private source/raw context in its original archive. Commit only normalized independent fixture outcomes and the selected replay decisions. The owner's ignored configuration loads private outcome fields locally. Never merge replay results into historical research denominators or describe the lexical rule gate as complete OpenSpec understanding.

Requirements OBS-009 / DEMO-006 and tasks 14.1–14.3 cover this increment. Evidence is in `docs/evidence/presentation-demo-2026-09-16.md`.
