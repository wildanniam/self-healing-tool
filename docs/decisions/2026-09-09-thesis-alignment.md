# Correct method drift before further empirical claims

### D25 — Owner requests thesis-method alignment

Wildan challenged the unsupported simplification and requested revision. The prior library was independently authored for portability/privacy, but that did not justify dropping research-method components. No evidence established the simplified method as better. D24 measured version 0.0.3, not a faithful re-run of the thesis. Its costs were bounded and all recoverable targets were in context, so neither token overflow nor missing cleansing is established as the cause of wrong selections.

Issue #11 restores the documented click/fill method in 0.0.5: cleaned bounded context and sparse-candidate fallback, thesis ranking signals/weights, original-locator context, stable selector suggestions and normalization, runtime validation feedback on retry. Independent new implementation avoids copying restricted historical source/artifacts. The parity matrix explicitly distinguishes restored mechanisms from retained safety/portability differences; do not claim exact prototype equivalence or improved accuracy from offline tests.

Existing narrow missing-locator trigger, provider fail-stop, strict JSON, exact one-match validation, independent semantics, privacy/oracle separation, time/size caps, and no code/patch execution remain. The prior prototype's additional read/select wrappers, application-framework-specific scans, unrestricted HTML and source patching are not silently added. Full-mode null selection retries within the configured limit, matching the thesis loop; exhausted null responses remain abstention, while provider failures still terminate immediately. Frame/shadow support remains out of scope.

No new paid batch is run. Preserve D24's private pinned package, frozen code and evidence. Restored features change actual model inputs, so subsequent empirical evaluation needs a separate frozen version/protocol and disclosure/budget review; do not reuse the fixed D24 batch or treat old approval as unlimited retries.
