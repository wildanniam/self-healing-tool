# Owner evidence and observation retry — 2026-09-14

### D29 — Implement and re-evaluate independently audited defects

The owner explicitly requests implementation of the discussed solutions, rerunning failed scenarios, and analysis of actual outcomes. This standard-risk increment uses [issue #17](https://github.com/wildanniam/self-healing-tool/issues/17), branch `codex/17-owner-evidence`, based on frozen D27 `da899299b79690e4a72480b0ccb4deab8e17648f`. No merge, release, submission or production changes are authorized by this increment.

D28 independently reproduced: nearest unnamed semantic wrappers can discard a real owner's identity; broad ancestor subtree text can lend another entity's identity to a control; null retries reuse unchanged observations. Hashes did not support a stale-build explanation. The lexical gate also cannot distinguish misleading identical labels or infer absent synonyms; these are separate method limitations, not all extraction defects.

Implement bounded owner evidence with provenance, separating local action-group text from owning identity. Use accessible names, own headings/legends or owned row identity; never search ancestors for contract/task words or merge descendant entity prose. Unnamed generic wrappers may be transparent; nested unresolved named boundaries return uncertainty. A missing inner structural owner cannot borrow outer identity. Keep originating-node locator verification.

After explicit no-selection, allow at most one bounded fresh observation per event. Another provider call is permitted only if actual selection evidence changes and the attempt/time budget remains. Record exact per-attempt inputs, refresh outcome and hashes; retain the initial context and all usage. Provider exceptions and spec refusal remain terminal.

Keep existing contracts, task descriptions, fixtures, independent native oracles, ranker weights and model prompts unchanged. Version the representation and null policy in 0.0.8. No certificate/shipment keywords, case IDs, answer locators, synonym patches, historical signature store or semantic-proof claim. Candidate selection changes cannot be causally attributed to one bundled feature from a before/after run alone.

The requested rerun uses a fresh bounded synthetic regression: the same 16 known conditions × A/B/C × 3 repeats = 144 planned slots, at most 432 provider requests and US$0.50 conservative reserved cost. Use the already selected gpt-4o-mini profile, no model fallback. Freeze source/package/config/harness inputs before dispatch. Budget exhaustion remains visible; no automatic top-up. No private-host collection is included in this bounded run. Preserve all D26/D27 evidence and prior closed ledgers. Offline forced-selector diagnostics are distinct from live model evidence. Reused conditions are not a fresh holdout or proof of generalization.

Completion requires engineering checks, native oracle/fixture preservation, exact-input and ledger audits, honest class-specific outcomes, remaining limitations, a linked PR and an Atlas checkpoint. Research interpretation and manuscript claims remain subject to owner review.
