# Candidate repair and bounded regression rerun

### D27 — authorized post-correction study

The owner explicitly requests implementing the discussed fixes, checking changes before testing, rerunning the previously failing scenarios, and evaluating the results. This new authorization supersedes prior no-new-batch guidance for this bounded D27 rerun only. Historical ledgers/results remain immutable; no merge, public release, production changes or manuscript submission is authorized.

Issue #15 owns standard engineering in this repository; private issue #182 owns the local repeat harness. D26 diagnostic evidence identifies nested host-text selector mismatch, unavailable action candidates, wrapper/structural ranking bias, duplicate-class penalties, and excessive candidate representation. Hidden soft penalties were also present in the original thesis; do not misstate that all hidden elements were excluded there.

Version0.0.7 changes shared core behavior and compact request shape explicitly. Model gpt-4o-mini, temperature0, output500, three attempts and3000/45000/15000ms action/recovery/provider limits remain the D26 settings. Context limits remain8000 candidate/HTML characters and12000 complete-request characters but measurement uses the actual compact projection. No candidate/oracle answer is hardcoded.

Operational envelope: one fresh apsec-d27-core-repair-2026-09-14-v1 ledger capped at US$2 and980 requests; phase ceilings baseline180, development360, holdout432, smoke8, conserving existing audited mechanics. The holdout phase name is historical: reused synthetic tasks are known regression evidence. Primary target is20 private conditions×3arms×3repeats=180 slots, plus the existing16 synthetic conditions×3×3=144 slots. No additional batch or post-outcome tuning of the frozen result is implied. Provider failure with unknown usage halts the ledger; never erase or silently top up it.

Offline verification and freeze precede live calls. Canonical D26 source/tasks/contracts/oracles remain unchanged. Report correct recovery, every wrong effect, unnecessary/appropriate refusal, normal controls, failures, latency and tokens/cost separately. A locator uniquely identifying its source node is not business-semantic proof. More candidates reaching admission can expose new false-healing risks; zero historical wrong effects is not a guarantee. Public artifacts remain generic and private-host data remain in their own repository.
