# D33 RM1 — verified offline results and interrupted live pilot

**Live evaluation is incomplete.** One pilot POST failed in transport without a model response or known usage. Eight pilot slots and all108 main slots remain unstarted. The ledger stopped as designed; no paid retry or ledger reset was performed.

## Offline findings

These are deterministic selection results for12 main conditions, not model recovery results.

| Ranking | Target extracted | Target ranked first | Target in top30 | Target in final input |
|---|---:|---:|---:|---:|
| R0 | 12/12 | 12/12 | 12/12 | 12/12 |
| R1 | 12/12 | 4/12 | 11/12 | 10/12 |
| R2 | 12/12 | 12/12 | 12/12 | 12/12 |

R0 lexical Jaccard and R2 full thesis ranking both retain the intended target in all12 cases. R1 without relational scoring retains10/12. In S-A-L its target is rank25, survives top30, then is removed by the shared input budget. In S-S-L it is rank33 and is removed by top30. Alltargets survive extraction. Crowded main targetpositions8/24/48/64 were fixed before any ranking/model outcome.

This supports a narrow contribution from using owner relations in ranking on these fixtures. It does **not** demonstrate that the full weighted formula is better than a simple lexical baseline. Model recovery may still occur when target representation is absent because task/oldselector clues remain; live correctness must be measured separately.

## Live incident and accounting

- First slot P-W-A-U / R0 / repeat1: provider transport failure after about10.56seconds. No output selector, response usage or browser recovery action.
- Later read-only connectivity checks succeeded; authenticated access to the pinned model returned200. Usage endpoint returned403. These later probes do not establish whether the first POST reached the provider or was billed.
- Actual total cost and input/output tokens: **unknown**, not zero. One nonrefundable ledger reservation is **US$0.00149685**; it is not a confirmed charge.
- The50 frozen files, payload/runtime hash and single ledger reservation were independently audited.
- Frozen `analyze.mjs` correctly fails its audit but sums missing usage as zero placeholders. That table is not used here; this post-interruption report explicitly preserves unknown values and leaves model effectiveness unmeasured. Original frozen files/results remain intact.

## Verification

51unit tests,88browser tests,7audit tests, typecheck, strict OpenSpec/traceability and clean-tarball consumer check passed. Nativepreflight359checks/105freshcontexts and45offline case-arm contexts passed. Independent reviewer verified ranking parity with D30 and corrected freeze/cleanup/async accounting guards before pilot. Passing engineering checks does not establish empirical effectiveness or guarantee absence of all defects.

## Remaining work

Reconcile unknown usage through the proper account; retain failed pilot and reservation. Any continuation needs a documented resume protocol because the frozen runner intentionally refuses an already-started phase, and the main requires a valid pilot audit. Preserve the original US$3/351request ceiling and all lineage; do not silently start another batch. Final recovery/cost/latency comparison remains open.

## Review and retained artifacts

Implementation [issue25](https://github.com/wildanniam/self-healing-tool/issues/25), branch `codex/rm1-dom-ranking`, source commit `554a7f31aa650c517228c13f2deb156b2466b4c3`; base D30 `12f6b2e843918e66085768ca8d267e98330a846a`. No merge, release or conference submission.

Runtime Node25.8.2, Playwright1.62.1/Chromium on macOS. `npm run typecheck`, `npm run test:unit` (51), `npm run test:browser` (88), `npm run test:audit` (7), `npm run check`, and `npm run test:consumer` passed. Sandbox browser/cache restrictions were resolved by running local browser/consumer checks with appropriate permissions and a temporary npm cache; no global permissions/cache ownership changed.

Native command `node evaluation/rm1/native-preflight.mjs` passed359checks; offline `node evaluation/rm1/offline.mjs` passed45contexts. The initial native358check report is superseded by359checks adding before/after source identity. Earlier timestamps remain retained. Pilot command `node evaluation/rm1/run.mjs --pilot --live` used explicit local environment loading and stopped after first transport failure. `node evaluation/rm1/analyze.mjs --pilot` correctly returned a failed completion audit; its unknown-cost placeholders are not reported as actual totals. `node evaluation/rm1/report-partial.mjs` produces the honest offline/interruption summary with null unknown totals.

Frozen source SHA256: `3106b4c8c57917fc3ef39fa8985902a587fc724ccdbd498baed33ff0ccfae99a`. Raw archive is local `output/d33/` (ignored by Git), with preflight reports, exact payload, frozen source/build, partial slot index, halted ledger, both independent audits and partial summary. No credential, browser login/session state or private application artifact is included. The stored report is auditable research evidence, not a completed main study.
