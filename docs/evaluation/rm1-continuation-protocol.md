# D33 RM1 continuation amendment — 18 September 2026

The owner explicitly requested analysis and execution after the interrupted-pilot continuation was explained. This amendment authorizes the engineering continuation of the same RM1 study under issue25/PR26. It does not alter the research question, cases, rankers, prompts, model, budgets per input, runtime timeouts, validation, retry semantics or effect oracle. Original `rm1-dom-ranking-protocol.md` and its50 frozen inputs remain byte-identical.

## Preserved incident and authorization

The original first pilot slot P-W-A-U/R0/repeat1 failed without a usable model response, usage or recovery action. Its ledger stays halted with actual cost/tokens null, original hash `3b73af76fdca63c2552192f7a66a283c17a800bfff624a95b0aa177dd3fa2834`. A separately authorized diagnostic succeeded with210 input/5 output tokens. Neither the dashboard's empty usage view nor that success reconciles billing for the earlier failure.

This is a declared, owner-authorized continuation with the historical unknown explicitly carried forward, **not a claim of genuine usage reconciliation** and not a reset of a halted ledger. It supersedes the earlier operational requirement to establish the old exact charge before any continuation, only for this one acknowledged historical incident. The original halt remains preserved. Any new unknown or pending request halts collection again; there is no automatic transport retry or fresh continuation on failure.

## Joint accounting

- Original failed request reservation: USD0.00149685, actual cost unknown.
- Diagnostic reservation: USD0.0003378, known rate-calculated cost USD0.0000345.
- Both reservations are retained conservatively. Two requests have consumed the original351-request allowance.
- Companion journal maximum:349requests and USD2.99816535 reserved, so combined reservations cannot exceed USD3. Pilot cap25 and main cap324. Pilot cannot borrow main allowance; reaching25 stops even if pilot slots remain.
- Overall actual study-effort cost remains null while the original unknown remains; separately report known-cost subtotal, unknown request count, and retained reservations. Continuation-phase costs may be reported when fully known. Reservations are not verified charges or a provider billing guarantee.

## Continuation identity and gates

Use only `output/d33/continuation-v1/` and unique prefixed trial IDs. The first of9 pilot trials links explicitly to the interrupted original trial; the other8 link to previously unstarted slots. The original trial remains a recorded infrastructure incident and is never overwritten or pooled into valid recovery performance. Main has108 planned trials with unchanged12conditions ×3arms ×3repeats and all outcomes retained. Pilot and diagnostic data are separate from main.

Before phase start, every dispatch and final audit, verify hash-anchored original50 inputs/archives, original ledger/failed record/status, diagnostic ledger/plan/result/payload, runtime identity and common configuration. The first repeated pilot dispatch must match original payload SHA256 `bc98ee7229404c6212cb2f480dbda34dce816f9afcf07138b42ef67329437472`. Trial/incident lineage stays outside the prompt. Reuse offline/native checks only after exact original-input parity is verified.

Freeze original inputs plus companion runner/common/preflight/analyzer/tests and this amendment before paid collection. Independent review and zero-API continuation tests must pass. A phase cannot start twice or overwrite prior evidence. No cases, weights or prompts may be tuned based on observed results.

The owner selected organization Vanie. Verify its organization identifier through the visible authenticated settings page and test read-only model access with that explicit organization header before paid collection. The companion transport wrapper adds only the verified organization header; serialized paid bodies remain unchanged. Freeze this identity and safe access evidence. Record sanitized transport cause/status/request ID without credentials or raw headers. If attribution cannot be verified, do not claim it was verified.

Pilot acceptance means complete, correctly linked transport/schema/reset/oracle/accounting evidence, not9 correct recoveries or R2 superiority. Legitimate refusal, wrong effect and execution failures are retained. Main dispatch requires a passed continuation pilot integrity audit. A new unknown/pending usage, hash mismatch, missing evidence, cap exhaustion or harness defect stops the phase, preserving unstarted slots.

## Analysis and delivery

The companion analyzer must independently recompute effects and coverage, verify exact payloads/request-to-ledger links and model identity, preserve missing values as null, and retain all108 planned main denominators. Report per-arm/condition/stratum outcomes, paired R2−R0 and R2−R1 comparisons across12conditions, actual tokens and timing with instrumentation caveats. Repeats are not independent applications. Separate continuation integrity PASS from incomplete historical billing reconciliation. The original methodological limitations and D30 separation remain.

Archive original and continuation artifacts without overwriting historical files; update evidence, issue-linked draft PR and Atlas Vault. No merge, release or conference submission is included.
